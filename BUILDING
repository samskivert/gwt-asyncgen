Building GWT AsyncGen
---------------------

The project is built with Maven (http://maven.apache.org/). To build and
install the library into your local Maven repository, execute:

% mvn clean install
